<%= form_with(model: sheep, local: true) do |form| %>
  <% if sheep.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sheep.errors.count, "error") %> prohibited this sheep from being saved:</h2>

      <ul>
      <% sheep.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description, t('.description') %>
    <%= form.trix_editor :description, id: :sheep_description %>
  </div>

  <div class="field">
    <%= form.label :status, t('.status') %>
    <%= form.text_field :status, id: :sheep_status %>
  </div>

  <div class="field">
    <%= form.label :priority %>
    <%= form.text_field :priority, id: :sheep_priority %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
	

	
<% end %>
